function add(a, b) {
  return a + b;
};

function divide (a, b) {
  return a / b;
};

function mod(a, b) {
  return a % b;
};

function multiply(a, b) {
  return a * b;
};

function subtract(a, b) {
  return a - b;
};

/**
 * js doc added
*/

module.exports = {
  add: add,
  divide: divide,
  mod: mod,
  multiply: multiply,
  subtract: subtract,
};
